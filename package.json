{
  "name": "votefeed-root",
  "private": true,
  "scripts": {
    "dev": "concurrently \"npm --prefix server run dev\" \"npm --prefix client run dev\""
  },
  "devDependencies": {
    "concurrently": "^9.0.0"
  },
  "dependencies": {
    "@openai/agents": "^0.4.7",
    "openai": "^6.21.0"
  }
}
